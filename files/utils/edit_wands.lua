function EditWands(player_entity)

    -- GamePrint("EditWands")



    -- local items = GameGetAllInventoryItems(player_entity)
    -- for _, item in ipairs(items) do
    --     -- GamePrint(item)
    --     -- GamePrint("item = " .. tostring(item) .. "  alive = " .. tostring(EntityGetIsAlive(item)))

    --     local tags = EntityGetTags(item)

    --     if string.find(tags, "wand") then

    --         local wand_capacity = EntityGetWandCapacity(item)

    --         -- GamePrint(tostring(wand_capacity))

    --     end
    -- end








    -- GamePrint("EditWands end")

end